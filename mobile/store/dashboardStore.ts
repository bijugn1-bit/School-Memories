import { create } from 'zustand'\n\ninterface DashboardState {\n  totalGroups: number\n  totalAlbums: number\n  totalPhotos: number\n  updateStats: (groups: number, albums: number, photos: number) => void\n}\n\nexport const useDashboardStore = create<DashboardState>((set) => ({\n  totalGroups: 0,\n  totalAlbums: 0,\n  totalPhotos: 0,\n  updateStats: (groups, albums, photos) =>\n    set({\n      totalGroups: groups,\n      totalAlbums: albums,\n      totalPhotos: photos,\n    }),\n}))\n